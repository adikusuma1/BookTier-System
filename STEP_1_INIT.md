## Instruksi Setup Frontend
1. Initialize Next.js app dengan TypeScript, Tailwind, ESLint.
2. Install Shadcn UI (`npx shadcn-ui@latest init`).
3. Install komponen: `button`, `input`, `card`, `badge`, `alert`, `skeleton`.
4. Install library: `axios`, `lucide-react`.